server:
  port: 9092

eureka:
  instance:
    hostname: localhost
    port: 9091
  client:
    registerWithEureka: true
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${eureka.instance.port}/eureka/
  server:
    wait-time-in-ms-when-sync-empty: 3000

security:
    oauth2:
        client:
            access-token-validity-seconds: 1800 
            authorized-grant-types: password,refresh_token
            client-id: 589974106770-dd836st9ucbeqr014pa878d2l759p00t.apps.googleusercontent.com
            client-secret: mvXDITlzGbmM85AZ5Oc2X7OT
            resource-ids: resources
            accessTokenUri: https://www.googleapis.com/oauth2/v4/token
            userAuthorizationUri: https://accounts.google.com/o/oauth2/v2/auth
            clientAuthenticationScheme: form
            scope: openid email profile
            resource:
              userInfoUri: https://www.googleapis.com/oauth2/v3/userinfo
              preferTokenInfo: true
spring:
    datasource:
        data: classpath:/sql/data.sql
        driver-class-name: com.mysql.jdbc.Driver  
        username: invilla
        password: '1234'
        platform: mysql
        schema: classpath:/sql/schema.sql
        url: jdbc:mysql://localhost:3306/oauth?autoReconnect=true&useSSL=false&useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC     
    jpa:
        database-platform: org.hibernate.dialect.MySQLDialect
        generate-ddl: false
        hibernate:
            ddl-auto: none
        show-sql: true
